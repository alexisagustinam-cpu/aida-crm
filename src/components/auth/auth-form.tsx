import { ActionSubmitButton } from '@/components/ui/action-submit-button'
import Link from 'next/link'
import { Wordmark } from '@/components/brand/wordmark'

export function AuthForm({ action, mode, error, message }: { action: (formData: FormData) => void | Promise<void>; mode: 'login' | 'signup'; error?: string; message?: string }) {
  const signup = mode === 'signup'
  return <main className="product-grid grid min-h-[100dvh] place-items-center p-5"><section className="w-full max-w-md border border-brand-border bg-brand-surface-elevated p-7 shadow-sm"><div className="text-2xl"><Wordmark /></div><p className="mono mt-8 text-[10px] uppercase tracking-[.16em] text-brand-primary">Acceso seguro / AIDA CRM</p><h1 className="mt-2 text-3xl font-bold tracking-[-.06em]">{signup ? 'Crea tu acceso.' : 'Bienvenido de vuelta.'}</h1><p className="mt-3 text-sm text-brand-muted">{signup ? 'Tu organización se configurará en el siguiente paso.' : 'Ingresa a tu espacio comercial.'}</p>{error && <p role="alert" className="mt-5 border-l-2 border-brand-danger bg-red-50 p-3 text-sm text-brand-danger">{error}</p>}{message && <p className="mt-5 border-l-2 border-brand-success bg-lime-50 p-3 text-sm text-brand-success">{message}</p>}<form action={action} className="mt-7 space-y-4"><label className="block text-sm font-medium">Email<input required name="email" type="email" autoComplete="email" className="mt-1.5 w-full border border-brand-border bg-brand-bg px-3 py-2.5 outline-none focus:border-brand-primary" /></label><label className="block text-sm font-medium">Contraseña<input required name="password" type="password" minLength={8} autoComplete={signup ? 'new-password' : 'current-password'} className="mt-1.5 w-full border border-brand-border bg-brand-bg px-3 py-2.5 outline-none focus:border-brand-primary" /></label><ActionSubmitButton className="w-full bg-brand-text px-4 py-3 text-sm font-semibold text-brand-bg hover:bg-brand-primary">{signup ? 'Crear cuenta' : 'Iniciar sesión'}</ActionSubmitButton></form><p className="mt-6 text-sm text-brand-muted">{signup ? '¿Ya tienes acceso?' : '¿Aún no tienes cuenta?'} <Link className="text-brand-primary underline underline-offset-4" href={signup ? '/login' : '/signup'}>{signup ? 'Inicia sesión' : 'Crear cuenta'}</Link></p></section></main>
}
